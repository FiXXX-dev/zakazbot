# config.js намеренно хранится в репозитории (нужен для GitHub Pages):
# в нём только публичные значения — URL и anon-ключ Supabase, режим "edge".
# НИКОГДА не коммитьте config.js с заполненным OPENAI_API_KEY (режим "direct").

# Зависимости и системный мусор
node_modules/
.DS_Store
*.log

# Supabase CLI
supabase/.temp/
supabase/.branches/
.env
.env.*
